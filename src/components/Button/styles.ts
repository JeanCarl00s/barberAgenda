import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        width: 100,
        height: 25,
        borderRadius: 8,
        backgroundColor: "#fff",
        margin: 10,

        display: "flex",
        alignItems: "center",
        justifyContent: "center"
    },
    Text: {
        fontFamily: 'Roboto',
        fontSize: 15,
        fontWeight: "bold"
    }
});