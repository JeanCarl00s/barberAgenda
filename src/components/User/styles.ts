import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",

        width: "100%",
        height: 200,
    },
    pic: {
        width: 90,
        height: 90,
        borderRadius: 45
    },
    name: {
        fontSize: 20,
        color: "#fff",
        padding: 10
    },
    
});